# Tools Tab - Manual QA Test Cases

## Test Cases

| # | Operation | Test | Steps | Pass Criteria |
|---|-----------|------|-------|---------------|
| **Copy Banks** | | | | |
| SM1 OK | Copy Banks | Copy single bank | Copy Bank A → Bank B (same project) | Bank B Parts + Patterns match Bank A |
| SM2 OK | Copy Banks | Copy to multiple banks | Copy Bank A → Banks B, C, D | All 3 destination banks match source |
| SM3 OK | Copy Banks | Cross-project copy | Copy Bank A from Project1 → Bank B in Project2 | Open Project2, verify Bank B data matches |
| SM84 | Copy Banks | Flex-track sample lock follows remap | Source has a Flex track with a sample-lock p-lock to flex slot N. Copy bank with samples to a project where that sample lands on a different flex slot | Locked step references the new flex slot (plays the same sample on hardware), not the old slot number |
| SM85 | Copy Banks | Static-track sample lock follows remap | Source has a Static track with a sample-lock p-lock to static slot N. Copy bank with samples so the sample relocates | Locked step references the new STATIC slot holding that sample; it does not follow the flex remap or stay at the old number |
| SM86 | Copy Banks | Sample lock to slot 1 follows remap | Source step locked to slot 1, copy bank with samples so slot 1's sample relocates | Lock follows to the new slot (slot 1 is a real lock, not treated as "no lock") |
| SM87 | Copy Banks | Sample lock slot shown 1-based | P-lock a step to sample slot N, open the step details in the pattern view | "Sample Slot:" shows N, matching the device's 1-based slot numbering (not N-1) |
| SM88 | Copy Banks | Reused slot wording | Copy a bank with samples whose files already exist in the destination (same filename) | Validation line and success message say "N already in destination and reused" (no "deduplicated" wording) |
| SM89 | Destination Modal | Browse a folder containing projects | In the destination selector, click "Browse..." and pick a folder that contains projects somewhere below it (not a project itself) | The folder is scanned recursively; a "Manual Browse" section lists every project found, and clicking one selects it as destination |
| SM90 | Destination Modal | Browse a project folder directly | Click "Browse..." and pick a folder that is itself an Octatrack project | The project is selected as destination immediately, modal closes |
| SM91 | Destination Modal | Browse a folder without projects | Click "Browse..." and pick a folder with no Octatrack project anywhere below it | Error message states no Octatrack project was found in the selected folder |
| SM67 OK | Copy Parts | Multi-select dest Banks | Copy Part 1 → Banks A, B, C | Part 1 copied to all 3 destination banks |
| **Copy Parts** | | | | |
| SM4 OK | Copy Parts | Copy single part | Copy Part 1 → Part 3 | Part 3 machines, amps, LFOs, FX match Part 1 |
| SM5 OK | Copy Parts | Copy All parts | Click source "All", execute | All 4 Parts copied including names |
| SM6 OK | Copy Parts | Cross-bank copy | Copy Part 1 Bank A → Part 2 Bank B | Bank B Part 2 matches Bank A Part 1 |
| SM7 OK | Copy Parts | Part names copied | Copy Part with custom name | Destination Part name matches source |
| SM8 OK | Copy Parts | Saved state copied | Copy a Part, check saved/unsaved states | Both saved and unsaved state copied to destination |
| SM9 OK | Copy Parts | Edited bitmask mirrored | Copy non-edited Part to destination | Destination edited bit cleared (mirrors source) |
| **Copy Patterns** | | | | |
| SM10 OK | Copy Patterns | Copy single pattern | Copy Pattern 1 → Pattern 5 | Pattern 5 trigs and data match Pattern 1 |
| SM11 OK | Copy Patterns | Copy All patterns 1-to-1 | Source All, execute | All 16 Patterns copied to destination 1-to-1 |
| SM12 OK | Copy Patterns | Copy single to multiple | Copy Pattern 1 → Patterns 5, 8, 12 | All 3 destination patterns match source Pattern 1 |
| SM13 OK | Copy Patterns | Cross-bank pattern copy | Copy Pattern 1 Bank A → Pattern 3 Bank C | Bank C Pattern 3 matches Bank A Pattern 1 |
| SM13b | Copy Patterns | Multi-bank pattern copy | Copy Pattern 1 → Banks A, B, C | Pattern 1 copied to all 3 destination banks |
| SM13c | Copy Patterns | All banks pattern copy | Click "All" bank button, execute | Pattern copied to all 16 banks |
| SM13d | Copy Patterns | Self-copy last ordering | Copy Pattern 1 Bank A → Banks A, B (same project) | Both banks updated; source bank (A) processed last to avoid overwriting before other banks read it |
| SM14 OK | Copy Patterns | Keep Original Part mode | Copy pattern with "Keep Original" | Copied pattern retains the destination pattern's existing Part assignment (not the source's) |
| SM15 OK | Copy Patterns | Copy Source Part mode | Copy pattern with "Copy Source" | Part data also copied, pattern references copied Part |
| SM16 OK | Copy Patterns | User Selection Part mode | Copy pattern with "User Selection" Part = Part 3 | Copied pattern assigned to Part 3 |
| SM17 OK | Copy Patterns | All Tracks copies everything | Copy pattern with "All Tracks" | All 16 tracks (T1-T8 + M1-M8) copied per Mode Scope |
| SM18 OK | Copy Patterns | Specific Tracks copies subset | Copy Pattern 1 with Specific Tracks (T1, T2 only) | Only T1, T2 trigs copied; other tracks untouched |
| SM19 OK | Copy Patterns | Audio scope copies only T1-T8 | Copy pattern with Mode Scope = Audio | Only audio track trigs copied; MIDI trigs untouched in destination |
| SM20 OK | Copy Patterns | Both scope copies all tracks | Copy pattern with Mode Scope = Both | Both audio and MIDI trigs copied |
| SM21 OK | Copy Patterns | MIDI scope copies only M1-M8 | Copy pattern with Mode Scope = MIDI | Only MIDI track trigs copied; audio trigs untouched in destination |
| **Copy Tracks** | | | | |
| SM22 OK | Copy Tracks | Audio single track | Copy T1 → T3 (Part params) | T3 Part params match T1 (both saved and unsaved state); Part name NOT copied (destination Part is a hybrid) |
| SM23 OK | Copy Tracks | Audio tracks to all | Source/Dest All Audio, execute | All 8 audio tracks copied |
| SM23 OK | Copy Tracks | Audio single to all tracks | Copy T1 → All (T1-T8) | T1 audio track data copied to all 8 audio tracks |
| SM24 OK | Copy Tracks | MIDI single track | Copy M1 → M2 (Part params) | MIDI params, arp sequences, custom LFO match source |
| SM25 OK | Copy Tracks | MIDI All tracks | Source/Dest All MIDI, execute | All 8 MIDI tracks copied |
| SM26 OK | Copy Tracks | Cross-bank track copy | Copy T1 Bank A → T3 Bank B | Bank B T3 matches Bank A T1 |
| SM26b | Copy Tracks | Multi-bank track copy | Copy T1 → T3, Banks A, B, C | T3 copied to all 3 destination banks |
| SM26c | Copy Tracks | All banks track copy | Click "All" bank button, execute | Track copied to all 16 banks |
| SM26d | Copy Tracks | Self-copy last ordering | Copy T1 → T3, Banks A, B (source=A, same project) | Both banks updated; source bank (A) processed last to avoid overwriting before other banks read it |
| SM27 OK | Copy Tracks | Part Parameters mode | Execute with "Part Parameters" mode | Machine types, amp, LFO, FX, volumes, recorder setup copied; pattern trigs unchanged |
| SM28 OK | Copy Tracks | Both mode | Execute with "Both" mode | Sound design AND pattern triggers copied |
| SM29 OK | Copy Tracks | Pattern Triggers mode | Execute with "Pattern Triggers" mode | Only step data copied (trigs, trigless, P-locks, swing); Part params unchanged |
| SM30 OK | Copy Tracks | All patterns 1-to-1 | Both mode, All patterns (default), T1 → T2 | All 16 patterns' T2 triggers match T1 |
| SM30b OK | Copy Tracks | 1-to-multiple patterns | Both mode, Pattern 1 → Pattern 1+2, T1 → T1+T3 | Triggers of patterns 1 and 2 of tracks 1 and 3 should match T1 |
| SM31 OK | Copy Tracks | Specific source to specific dest pattern | Both mode, Pattern 1 → Pattern 5, T1 → T2 | T2 triggers in Pattern 5 match T1 triggers in Pattern 1 |
| SM32 OK | Copy Tracks | Source pattern to All dest patterns | Both mode, source Pattern 3 → dest All, T1 → T2 | All 16 dest patterns' T2 match source Pattern 3's T1 |
| SM33 OK | Copy Tracks | Machine types copied | Copy T1 Part params → T3 | Destination machine type matches source |
| SM34 OK | Copy Tracks | FX settings copied | Copy track with FX1/FX2 configured | FX parameters match source |
| SM35 OK | Copy Tracks | Volumes and amp copied | Copy track with custom volume/amp | Volume and amp settings match source |
| SM36 OK | Copy Tracks | Custom LFO copied | Copy track with custom LFO design | Custom LFO waveform and interpolation masks match source |
| SM37 | Copy Tracks | Recorder setup copied | Copy track with recorder configuration | Recorder source and settings match source |
| SM37b OK | Copy Tracks | Multi-select dest Parts | Copy T1 → T3, select dest Parts 1 and 3 | T3 params copied to both Part 1 and Part 3 in destination |
| **Copy Sample Slots** | | | | |
| SM38 OK | Copy Sample Slots | Copy single slot | Copy slot 1 → slot 2 (diff project) | Slot data matches source; dest slot_id = 2 (not source's 1) |
| SM39 OK | Copy Sample Slots | Copy slot range | Copy slots 1-10 → slots 50-59 | All 10 slot assignments copied; each dest slot_id matches its position |
| SM40 OK | Copy Sample Slots | Copy all 128 slots | Select All (1-128), execute | All 128 slots copied |
| SM41 OK | Copy Sample Slots | Self-copy same project | Copy slot 1 → slot 2 (same project) | Slot 2 matches slot 1 |
| SM42 OK | Copy Sample Slots | Static + Flex | Select "Static + Flex", copy slots, execute | Both slot types copied |
| SM43 OK | Copy Sample Slots | Static only | Select "Static", copy slots, execute | Only Static slot data copied; Flex untouched |
| SM44 OK | Copy Sample Slots | Flex only | Select "Flex", copy slots, execute | Only Flex slot data copied; Static untouched |
| SM83 OK | Copy Sample Slots | Execute disabled on overflow | Range mode, source 1-50, set dest start to 100 | Warning "Some slots will overflow" shown; Execute button disabled with tooltip |
| **Sample Assignments** | | | | |
| SM-SA1 OK | Copy Sample Slots | Assignments Copy — paths copied | Assignments=Copy, execute | Destination slot paths match source paths |
| SM-SA2 OK | Copy Sample Slots | Assignments Don't Copy — paths untouched | Assignments=Don't Copy, Attributes=Copy, execute | Destination slot paths unchanged; only attributes updated |
| SM-SA3 OK | Copy Sample Slots | Both Don't Copy — Execute disabled | Set both Assignments and Attributes to Don't Copy | Execute button disabled with tooltip "Select at least one…" |
| SM-SA4 | Copy Sample Slots | Missing source files warning | Assignments=Copy, source slots reference missing .wav files | Warning badge shows "N missing file(s)" next to Audio Files label |
| SM-SA5 | Copy Sample Slots | Warning hidden when Assignments=Don't Copy | Set Assignments=Don't Copy | No missing files badge visible |
| **Audio Files (sub-option of Assignments=Copy)** | | | | |
| SM-AF1 | Copy Sample Slots | Mirror locations — local files copied | Source has project-local file. Assignments=Copy, Audio=Mirror | File copied to dest project root; dest path = filename |
| SM-AF2 | Copy Sample Slots | Mirror locations — pool refs preserved | Source has ../AUDIO/ reference. Assignments=Copy, Audio=Mirror | Dest path = ../AUDIO/filename (no file copy) |
| SM-AF3 OK | Copy Sample Slots | Copy all to project | Assignments=Copy, Audio=Copy all to project | Audio files copied to dest project root (flat, by filename); slot paths updated to filename only |
| SM-AF4 OK | Copy Sample Slots | Move all to Pool | Assignments=Copy, Audio=Move all to Pool (same Set) | Files in AUDIO dir, slot paths updated to ../AUDIO/ in both source and destination projects |
| SM-AF5 OK | Copy Sample Slots | Move all to Pool deletes originals | Move all to Pool, check source folder | Original .wav files deleted from source (except files also referenced by opposite slot type) |
| SM-AF6 OK | Copy Sample Slots | Move all to Pool requires same Set | Dest project in different Set | Move all to Pool unavailable (disabled) |
| SM-AF6b | Copy Sample Slots | Mirror locations requires same Set | Dest project in different Set | Mirror locations unavailable (disabled) |
| SM-AF7 OK | Copy Sample Slots | Audio mode auto-switches on dest change | Select Mirror or Move all to Pool, change dest to diff-Set project | Switches to "Copy all to project" automatically |
| SM-AF8 OK | Copy Sample Slots | Shared file kept on Move to Pool | Move to Pool with file shared between Static and Flex | Shared file NOT deleted; success message shows count of kept files |
| SM-AF9 OK | Copy Sample Slots | Copy from AUDIO pool preserves source | Copy slots referencing ../AUDIO/ files to another project in same Set | Source files in AUDIO pool intact (not 0 bytes); dest files in project root |
| SM-AF10 | Copy Sample Slots | .ot files never copied with audio | Assignments=Copy, any audio mode, source slot has .ot file | Only .wav copied; .ot file NOT present in destination |
| SM-AF11 | Copy Sample Slots | Move to Pool reintegrates .ot | Move to Pool, source slot has .ot sidecar file | .ot data re-integrated into source project.work + markers.work; .ot file deleted |
| **Sample Attributes** | | | | |
| SM-AT1 OK | Copy Sample Slots | Attributes Copy — all selected by default | Attributes=Copy, all attribute buttons selected (default), execute | All AED settings (gain, BPM, timestretch, loop, trig quant, trim, loop point, slices) copied |
| SM-AT2 OK | Copy Sample Slots | Attributes Don't Copy — dest attrs untouched | Attributes=Don't Copy, Assignments=Copy, execute | Destination slot attributes preserved (gain, BPM, etc. unchanged from destination values) |
| SM-AT3 OK | Copy Sample Slots | Selective attributes — gain only | Attributes=Copy, only "Gain" selected, execute | Only gain copied; BPM, loop, trim etc. remain at destination values |
| SM-AT4 OK | Copy Sample Slots | Selective attributes — markers only | Attributes=Copy, only "Trim points" + "Slices" selected, execute | Only trim and slices markers copied; gain, BPM, loop mode unchanged |
| SM-AT5 OK | Copy Sample Slots | None button deselects all | Click "None" button | All attribute buttons deselected |
| SM-AT6 OK | Copy Sample Slots | All button selects all | Click "None" then "All" | All attribute buttons re-selected |
| SM-AT7 | Copy Sample Slots | .ot priority for attributes | Source slot has .ot file with gain=90, project.work has gain=50. Attributes=Copy with Gain selected | Destination gets gain=90 (.ot value takes priority) |
| SM-AT8 | Copy Sample Slots | Fallback to project.work when no .ot | Source slot has no .ot file, project.work has gain=80. Attributes=Copy with Gain selected | Destination gets gain=80 (project.work fallback) |
| SM-AT9 | Copy Sample Slots | Attributes written to work files only | Copy attributes to dest | Dest project.work and/or markers.work updated; no .ot file created in destination |
| SM-AT10 OK | Copy Sample Slots | Selective: loop_point only | Attributes=Copy, only "Loop point" selected, execute | Only loop_point copied in markers.work; trim, slices, project.work attrs unchanged |
| SM-AT11 OK | Copy Sample Slots | Assignments OFF + Attributes ON | Assignments=Don't Copy, Attributes=Copy, execute | Dest path unchanged; selected attributes copied from source |
| SM-AT12 OK | Copy Sample Slots | Assignments ON + Attributes OFF | Assignments=Copy, Attributes=Don't Copy, execute | Path copied; dest attributes and markers preserved at original values |
| SM-AT13 OK | Copy Sample Slots | Copy slot with slices | Source slot has 16 slices. Attributes=Copy with Slices selected | All slice data (start/end/loop per slice + count) transferred correctly |
| SM-AT14 OK | Copy Sample Slots | Raw field preservation | Source has TRIGQUANTIZATION=-1. Copy with trig_quant selected | Dest gets TRIGQUANTIZATION=-1 (not corrupted to 255); TRIM_BARSx100 preserved |
| SM-AT15 OK | Copy Sample Slots | No spurious BPMx24 | Source slot has no BPMx24 line. Copy with bpm selected | Dest preserves its existing BPMx24 value (no spurious 2880 written) |
| SM-AT16 | Copy Sample Slots | Recording buffers not affected | Copy flex slots 1-128 between projects | Recording buffers (flex slots 129-136) are untouched in both source and destination |
| **Backup Feature** | | | | |
| SM75 OK | All operations | Backup before copy execute | Execute any copy operation | `backups/` directory created in dest project with timestamped subfolder containing destination files |
| SM81 OK | Copy Sample Slots | Backup source on Move to Pool | Execute Move to Pool | Source project `backups/` contains `project.work` and audio files that were moved/deleted; AUDIO pool files not backed up |
| SM76 OK | All operations | Backup on edit mode ON | Toggle View→Edit mode | `backups/` directory created with current bank file backed up |
| SM78 OK | All operations | Backup backs up dest audio by filename | Execute copy_sample_slots with audio mode Copy | Backup contains destination audio files (by filename) that would be overwritten |
| SM79 OK | All operations | Backup label in directory name | Execute different operations | Each backup dir name ends with operation label (e.g., `_copy_bank`, `_edit_mode`) |
| **Additional Tests** | | | | |
| SM74 | All operations | Auto-refresh on same-project copy | Copy any operation to current project | Project data refreshes automatically after successful copy |
| SM82 | Copy Sample Slots | Source project refreshes after Move to Pool | Execute Move to Pool to different project | Source (active) project UI refreshes to show updated ../AUDIO/ slot paths |
| **Fix Missing Samples** | | | | |
| FMS-01 | Fix Missing Samples | Status badge shows correct count | Select "Fix Missing Samples" from dropdown | Status badge shows number of unique missing files with Flex/Static breakdown |
| FMS-02 | Fix Missing Samples | Missing files table | Click "Show missing files" collapsible | Table shows filename and Slots column (Flex/Static/Both). "Both" highlighted |
| FMS-03 | Fix Missing Samples | Execute disabled when 0 missing | Select "Fix Missing Samples" when all sample files exist | Execute button not shown. Status badge shows 0 with green styling |
| FMS-04 | Fix Missing Samples | Pool options visible only with Set | Open project in a Set (parent has AUDIO dir) | "If found in Audio Pool" toggle visible. "If found in another project of Set" toggle visible |
| FMS-05 | Fix Missing Samples | Pool options hidden for non-Set project | Open project not in a Set (parent has no AUDIO dir) | Neither pool-related toggle shown. "Other Set projects" option not displayed |
| FMS-06 | Fix Missing Samples | Search step 1 — Project directory | Place missing file in project subdirectory. Click Execute | Step 1 spinner → checkmark with count. File resolved with "Update path" action |
| FMS-07 | Fix Missing Samples | Search step 2 — Audio Pool | Place missing file in AUDIO/ directory. Click Execute | Step 2 finds file. With "Use from Pool": path update to ../AUDIO/. With "Copy to Project": file copied |
| FMS-08 | Fix Missing Samples | Search step 3 — Other Set projects | Project in a Set. Place missing file in sibling project. Click Execute | "Other Set projects" step finds file. With "Copy to Project": file copied. With "Move to Pool": file moved to AUDIO/ |
| FMS-08b | Fix Missing Samples | Search step 3 — Parent directory | Project NOT in a Set. Place missing file in sibling project. Click Execute | "Parent directory" step finds file (instead of "Other Set projects"). File copied to project root |
| FMS-09 | Fix Missing Samples | Browse button — user directory search | Create missing files not in project/pool/siblings. Click Execute. Click "Browse..." on summary line | Directory picker opens. After selecting, new "User selection: *DirName*" step appears (dir name in italic) with found count. Browse button remains if files still missing |
| FMS-10 | Fix Missing Samples | Browse — tooltip shows full path | After browsing a directory, hover over the italic directory name in "User selection: *DirName*" step | Tooltip shows full directory path |
| FMS-11 | Fix Missing Samples | Same directory warning | Browse same directory twice | Warning badge appears in header with triangle icon, auto-fades after 2 seconds. Long paths trimmed from beginning |
| FMS-12 | Fix Missing Samples | Confirmation — path update only | File found in project subdirectory | Green "Update path → subdir/filename" in confirmation table |
| FMS-13 | Fix Missing Samples | Confirmation — copy from other project | File found in sibling project, "Copy to Project" selected | Blue "Copy to project ← ProjectB/filename" in confirmation |
| FMS-14 | Fix Missing Samples | Confirmation — move to pool | File found in sibling project, "Move to Pool" selected | Purple "Move to Pool + update path" in confirmation. "Other projects affected" section visible |
| FMS-15 | Fix Missing Samples | Confirmation — not found section | Some files not found anywhere | Red "Not found" section lists unresolved filenames |
| FMS-16 | Fix Missing Samples | Cancel from confirmation | Click Cancel on confirmation screen | Modal closes. No files modified. No backups created |
| FMS-17 | Fix Missing Samples | Apply changes — backup creation | Click "Apply Changes" on confirmation | Backup directory created under project/backups/ with label "fix_missing_samples" |
| FMS-18 | Fix Missing Samples | Apply changes — file copy | Apply with "Copy to Project" resolutions | Audio files copied to project root (.ot NOT copied — project has own AED data). Slot paths updated |
| FMS-19 | Fix Missing Samples | Apply changes — move to pool cross-project | Apply with "Move to Pool" from sibling project | File in AUDIO/. Original deleted from sibling. All sibling project paths updated |
| FMS-20 | Fix Missing Samples | Final summary | After Apply completes | Resolved count and not-found count displayed. Expandable details show per-file actions. Close returns to Tools panel |
| FMS-21 | Fix Missing Samples | .ot companion files NOT copied | Missing file has .ot companion in found location | Only .wav is copied/moved. .ot is never imported (project has own AED data in project.work + markers.work) |
| FMS-22 | Fix Missing Samples | Auto-apply (Skip Review ON) | Enable "Skip Review" option (default). Execute fix with all samples found | Search completes → Apply starts automatically (no confirmation screen). Done phase shows resolved count |
| FMS-23 | Fix Missing Samples | Manual review (Skip Review OFF) | Enable "Review before applying". Execute fix | Search completes → progress view with Browse/Review buttons. Click "Review changes" → confirmation table shown. Must click "Apply Changes" to proceed |
| FMS-24 | Fix Missing Samples | Previous button in confirmation | In confirmation screen, click "Previous" | Returns to progress view with Browse/Review buttons. Can click "Review changes" to go back to confirmation |
| FMS-25 | Fix Missing Samples | Pool subdirectory paths | Missing file found in AUDIO pool subdirectory (e.g., AUDIO/TECHNO/Atmos/file.wav). "Use from Pool" selected | Slot path updated to ../AUDIO/TECHNO/Atmos/file.wav (preserving subdirectory structure), not just ../AUDIO/file.wav |
| FMS-26 | Fix Missing Samples | Project refresh after apply | Apply changes successfully. Click Done to close modal | Tools panel refreshes missing count (should show 0 or reduced count). Sample Slots table reflects updated paths. No manual reload needed |
| FMS-27 | Fix Missing Samples | Both Flex and Static slots updated | Missing file referenced by both Flex slot(s) and Static slot(s). Apply fix | Both Flex and Static slot paths updated. Missing count decreases for both types |
| FMS-28 | Fix Missing Samples | Done phase progress view | Complete a fix (auto-apply or manual) | Done phase shows all search steps with checkmarks/skip icons and found counts. Summary shows "N/M missing files found". Done button closes modal |
| FMS-29 | Fix Missing Samples | Steps list scrolls with many directories | Browse 10+ directories via Browse button | Steps list becomes scrollable (max height). Summary, Browse, and action buttons remain fixed below |
| **Purge Project Samples** | | | | |
| PRG-01 | Purge Project Samples | Status button shows count and opens preview | Select "Purge Project Samples". Wait for scan | Status button shows "N unused audio files - of M scanned". Click it: "Unused Project Samples" modal opens listing Slot/Name/Location/Origin/Size, read-only, no changes made |
| PRG-01b | Purge Project / Audio Pool Samples | Scan status shows a live percentage | Select "Purge Project Samples" or "Purge Audio Pool Samples" on a project/pool large enough for the scan to be observable | While scanning, status shows "Scanning ...% " with the percentage climbing from 0 to 100 as the background scan calls resolve, then switches to the unused file count |
| PRG-02 | Purge Project Samples | Zero unused files | Select operation when every audio file in the project is referenced | Status shows green "0" with label "every sample in this project is referenced". No Execute button shown |
| PRG-03 | Purge Project Samples | Execute opens review modal | With unused files present, click Execute | "Review planned changes - N item(s)" modal opens with the same Name/Origin/Size table; Cancel and "Apply Changes" buttons shown |
| PRG-04 | Purge Project Samples | Delete mode forces review | Select "Delete files" (not the default - see PRG-24) | "Review before applying changes" checkbox is checked and disabled (cannot be turned off) |
| PRG-05 | Purge Project Samples | Move mode allows skipping review | "Move files to folder" is selected (default) | "Review before applying changes" checkbox is enabled, checked by default; unchecking it and clicking Execute applies the move immediately with no review screen |
| PRG-06 | Purge Project Samples | Default Move destination fallback chain | Select "Move files to folder" for the first time this session | Destination field pre-fills with the first of Downloads / Desktop / home directory / project's Set root (or project dir if standalone) that exists on disk |
| PRG-07 | Purge Project Samples | Browse overrides default destination | In Move mode, click "Browse..." and pick a folder | Destination field updates to the picked folder and is used for the move instead of the resolved default |
| PRG-08 | Purge Project Samples | Delete mode sends to Trash | Apply Changes with "Delete files" selected | Files/directories removed from their location and recoverable from the OS Trash Bin; done summary shows "N file(s) and M director(ies) removed (X reclaimed)" |
| PRG-09 | Purge Project Samples | Move groups files by origin subfolder | Apply Changes with "Move files to folder" | Files land under `<destination>/Unused Audio/<ProjectName>/<relative path>`, preserving subdirectory structure |
| PRG-10 | Purge Project Samples | Move auto-suffixes name collisions | Destination already contains a same-named file at the target path. Apply Changes with Move mode | Moved file is renamed with an auto-incrementing " (2)", " (3)"... suffix instead of the move failing |
| PRG-11 | Purge Project Samples | Clear unused sample slot assignments changes preview count | A slot has a file loaded but no machine assignment/p-lock. Toggle "Clear unused sample slot assignments" on | Unused file count increases to include that slot's file (live preview, nothing mutated yet) |
| PRG-12 | Purge Project Samples | Post-execute slot state after clearing | With slot-clearing enabled, Apply Changes | Done summary shows "N unused sample slot assignment(s) cleared"; project.work backed up first; the [SAMPLE] block for that slot is gone (slot no longer references any path) |
| PRG-13 | Purge Project Samples | Exclude backups/ directory default-on | Place an unused audio file only inside the project's `backups/` directory. Select the operation | File is not included in the unused count/preview (checkbox defaults checked). Unchecking it includes the `backups/` file in the scan |
| PRG-14 | Purge Project Samples | Directory collapse | Every audio file inside a subfolder is unused (recursively), while the project root itself keeps at least one referenced file | Preview/review shows one Directory row (folder icon + file count) instead of one row per file; the project root folder is never itself collapsed into a row |
| **Purge Audio Pool Samples** | | | | |
| PRG-15 | Purge Audio Pool Samples | Appears in operation dropdown, status/preview | Open Audio Pool Tools tab, select "Purge Audio Pool Samples" | Status button shows "N unused audio files - of M scanned"; click opens "Unused Audio Pool Samples" preview modal listing Slot/Name/Location/Origin/Size |
| PRG-16 | Purge Audio Pool Samples | Include all projects of set reveals nested options | Toggle "Include all projects of set" on | "Clear unused sample slot assignments" and "Exclude backups/ directory" (checked by default) sub-options appear nested; unchecking hides them again |
| PRG-17 | Purge Audio Pool Samples | Review modal, Delete to Trash, refresh | With unused pool files present, Execute → Apply Changes with "Delete files" | Review modal shows Name/Origin/Size table; after apply, files are sent to Trash and the pool usage + destination file listing both refresh automatically |
| PRG-18 | Purge Audio Pool Samples | Default Move destination fallback (pool) | Select "Move files to folder" for the first time this session | Destination pre-fills with the first of Downloads / Desktop / home directory / Set root (pool's parent directory) that exists on disk |
| PRG-19 | Purge Audio Pool Samples | Directory collapse (pool scope) | Every audio file inside a pool subfolder is unused, while the AUDIO/ root itself keeps at least one referenced file | Preview/review shows one Directory row for the subfolder; the AUDIO/ root itself is never collapsed into a row |
| PRG-20 | Purge Audio Pool Samples | Clearing slots across included projects can free pool files | Enable "Include all projects of set" + "Clear unused sample slot assignments". A pool file's only reference anywhere is a loaded-but-untriggered slot in one included project | That pool file appears in the unused preview/count (it becomes purgeable once the referencing slot is cleared) |
| PRG-21 | Purge Project Samples | .ot sidecar backed up before removal | An unused file has a same-named `.ot` sidecar. Apply Changes (Delete or Move) | The `.ot` is copied into the project's `backups/` directory before being trashed/moved, alongside any slot-clear backup from the same run |
| PRG-22 | Purge Project / Audio Pool Samples | Shared .ot left untouched when a sibling survives | Two files share a stem but differ in extension (e.g. `kick.wav` + `kick.flac`), both sharing one `.ot`. Only one of them is unused and gets purged | The still-referenced sibling's file is left alone as expected; its shared `.ot` sidecar is also left untouched (not trashed or moved) |
| PRG-23 | Purge Project / Audio Pool Samples | Shared .ot purged once when both siblings are purged | Both same-stem/different-extension siblings sharing one `.ot` are unused and included in the same purge | Apply Changes completes with no errors; both files and the single shared `.ot` are removed/moved exactly once |
| PRG-24 | Purge Project / Audio Pool Samples | Move is the default action | Select "Purge Project Samples" or "Purge Audio Pool Samples" for the first time | "Move files to folder" is selected by default, not "Delete files" |
| PRG-25 | Purge Project / Audio Pool Samples | Unused file count counts files, not rows | A subfolder with 6 unused audio files fully collapses into one Directory row, plus 15 more unused files sit outside it | Status count reads 21 unused audio files (15 + 6), not 16 (which would be counting the collapsed directory as a single item) |
| PRG-26 | Purge Project / Audio Pool Samples | Slot ID column shows loaded-but-unused slots | Enable "Clear unused sample slot assignments" so a loaded-but-untriggered slot's file becomes unused | Preview/review table's first column ("Slot") shows the slot label (e.g. "S1", "F2") for that file; files never loaded into any slot show "—" |
| PRG-27 | Purge Project / Audio Pool Samples | Directory rows are collapsible | In the preview or review table, click the caret next to a collapsed-directory row's folder icon | The directory's tree-style child file rows (each showing its own Slot/Name/Size) hide; clicking the caret again shows them again |
| PRG-28 | Purge Project / Audio Pool Samples | Tree-child rows show their own size | Expand a Directory row that absorbed multiple audio files of different sizes | Each child file row shows its own Size value (not blank, not the directory's total) |
| PRG-29 | Purge Project / Audio Pool Samples | Right-click context menu on any row | Right-click a top-level File row, a Directory row, and one of its tree-child file rows | Each shows a context menu with "Open in file explorer" (reveals that exact path) and "Copy file path" (copies that exact path to the clipboard) |
| PRG-30 | Purge Project / Audio Pool Samples | Review modal matches the preview modal | Open the preview list (status button), close it, then click Execute to open the review modal | Both modals are the same width; the table's Name column fills the remaining space in both instead of being narrower in the review modal |
| PRG-31 | Purge Project / Audio Pool Samples | Modals keep a minimum height on a short plan | On a project/pool whose purge plan is a single row (for example one directory standing in for many files), open the preview list, then close it and click Execute to open the review modal | Neither modal collapses to a squat strip just taller than its header; both keep the same minimum height, with empty space below the single row |
| PRG-32 | Purge Project / Audio Pool Samples | Non-audio files inside a collapsed directory are listed | A directory that fully collapses also contains non-audio files (artwork, readme, `.ot` sidecar) | The preview and review tables list those files as tree-child rows too, visually distinct from the audio rows, each with its own size |
| PRG-33 | Purge Project / Audio Pool Samples | Non-audio files are counted separately | Same setup as PRG-32, with 2 unused audio files and 3 non-audio files in the collapsed directory | Status button reads "2 unused audio files - of N scanned + 3 related files" (the related count trails the scanned total); the review banner reads "2 audio files + 3 related files"; the directory row reads "(2 audio + 3 other files)"; the audio headline count never absorbs the non-audio ones |
| PRG-34 | Purge Project / Audio Pool Samples | Directory holding only non-audio files is never purged | A subfolder contains no audio files at all, just documents/images | That subfolder never appears in the preview or review list, whatever else is being purged |
| PRG-35 | Purge Project / Audio Pool Samples | Review banner is centered and color-coded per action | Open the review modal once with "Move files to folder" selected, then again with "Delete files" | Move shows a centered orange banner reading "will be moved to <destination folder>"; Delete shows a centered red banner reading "will be sent to the Trash Bin". Both show the audio count, any "+ N related files" tail at the same size as the audio count, and the item/directory/size line |
| PRG-36 | Purge Project / Audio Pool Samples | Confirmation counts files inside removed directories | Purge a plan whose only row is a directory collapsing 50 audio files | Done screen reads "50 audio files (+ N related files if any), across 1 directory," - never "0 files and 1 directory removed" |
| PRG-37 | Purge Project / Audio Pool Samples | Confirmation reflects the chosen action | Complete a purge once in Move mode and once in Delete mode | The summary is three lines: a bolded "N audio files (+ N related files), across N directories," then "moved to <destination folder>" (or "sent to the Trash Bin" for Delete), then "<size> reclaimed" |
| PRG-38 | Purge Project Samples | Cleared slots are fully reset, not just unlinked | Enable "Clear unused sample slot assignments" and purge. Then inspect the affected slots in the Sample Slots table | Each cleared slot is completely empty - no leftover gain, loop mode, timestretch or trim attributes from the old sample - and is ready to host a new sample |
| PRG-39 | Purge Project / Audio Pool Samples | Scanned total is the real file count | Note the "of N scanned" figure on the status button, then count the audio files actually present in the project (or pool, plus set projects when "Include all projects of set" is on) | N matches the real total of audio files walked, not the number of unused findings |
| PRG-40 | Purge Project / Audio Pool Samples | .ot sidecar of a lone file is listed | An unused audio file has a same-named `.ot` sidecar and no surviving same-stem sibling | The file's row has an expand caret; expanding it shows the `.ot` as a tree-child row with its own size, and it counts toward the "other files" total |
| PRG-41 | Purge Project / Audio Pool Samples | Directory row wording | Open the preview or review list on a collapsed directory holding 110 audio files and 13 other files | The row reads "(110 audio + 13 other files)" |
| PRG-42 | Purge Project / Audio Pool Samples | Context menu closes on a left click | Right-click any row to open its context menu, then left-click somewhere else in the modal (the title, another row, empty space) | The menu closes |
| PRG-43 | Purge Project / Audio Pool Samples | Search finds files inside directories | Type the name of a file that sits inside a collapsed directory into the search box | The directory row stays listed with that file shown beneath it; non-matching children are hidden. Searching the folder's own name shows all of its children |
| PRG-44 | Purge Project / Audio Pool Samples | Progress modal during execution | Apply Changes on a plan with several items (ideally a slow move, e.g. to a destination on another drive) | The modal shows "Moving N / M" (or "Sending to Trash N / M"), the name of the item currently being handled, and a Cancel button - not just a bare spinner |
| PRG-45 | Purge Project / Audio Pool Samples | Cancel stops between items, never mid-file | Start a purge of several items and click Cancel partway through | The label changes to "Finishing the current item, then stopping..." and Cancel greys out. The run stops after the item in flight: already-processed items are fully moved/trashed, remaining items are untouched at their original location, and no file is left half-copied or partially written |
| PRG-46 | Purge Project / Audio Pool Samples | Cancelled run reports what it actually did | Cancel a purge partway, then read the done screen | It is prefixed "Cancelled" and the counts/size cover only the items processed before the cancel, not the whole plan |
| PRG-47 | Purge Project / Audio Pool Samples | One failing item does not abort the batch | Make one item impossible to remove (e.g. permissions), then purge a plan containing it plus several healthy items | Every other item is still processed; the failure is listed under the errors section of the done screen rather than aborting the whole run |
| PRG-48 | Purge Project / Audio Pool Samples | Review checkbox tooltip | Hover "Review before applying changes" with "Move files to folder" selected, then with "Delete files" selected | Move shows "Show the review screen listing planned changes before applying them"; Delete shows the same text followed by " - Required when deleting files" |
| PRG-49 | Purge Project / Audio Pool Samples | Item count matches visible rows | Open the preview or review list on a plan with a collapsed directory and/or a file carrying an `.ot` sidecar | "Showing X of Y items" counts the table rows actually on screen (parents plus their expanded children), and X drops when a directory is collapsed or a search/filter narrows the list |
| PRG-50 | Purge Project / Audio Pool Samples | Format column | Open the preview or review list | A "Format" column exists but is hidden by default; enabling it via the column menu shows "Audio" for audio files, "Other" for swept-along files, and a dash for directory rows. It sorts and filters like the other columns |
| PRG-51 | Purge Project / Audio Pool Samples | Format filter narrows the tree | Enable the Format column, then filter to "Other", then to "Audio" | "Other" lists only the non-audio files, keeping each parent row as context; "Audio" lists only audio files, so a lone file stays listed but its `.ot` child disappears. The active filter shows as a "Format: ..." badge that Reset clears |
| PRG-52 | Purge Project / Audio Pool Samples | Slot column sorts numerically | On a plan with slots S1, S9, S10, S99 and F2 (enable "Clear unused sample slot assignments" so loaded-but-untriggered slots appear), click the Slot column header | Order is F2, S1, S9, S10, S99 - not the text order S1, S10, S99, S9. Clicking again reverses it |
| PRG-53 | Purge Project / Audio Pool Samples | Slot-less rows stay last | Same setup as PRG-52, with at least one file that no slot references (shown as a dash) | The dash rows sit at the bottom in both sort directions - "no slot" never sorts to the top as if it were a low value |
| PRG-54 | Fix Project / Audio Pool Samples | Slot column sorts numerically | Open either Fix modal's list or review table on files spanning slots S1, S9, S10, S99 and click the Slot header | Same numeric ordering and dash-last behaviour as PRG-52/PRG-53 |
| PRG-55 | Purge Project Samples | Slots pointing at Audio Pool files are listed | Open a project where several unused slots hold samples from the Audio Pool (Source column reads "Audio Pool"), enable "Clear unused sample slot assignments", then open the preview list | Every one of those slots appears as its own row with its slot label, file name, "Audio Pool" origin and size, and an Action of "Clear slot only" - not just the project-local files being removed |
| PRG-56 | Purge Project Samples | Pool files are cleared but never removed | Same setup as PRG-55, then Execute and Apply Changes | The slots are emptied in the project, but the Audio Pool files themselves are still on disk and untouched - only "Purge Audio Pool Samples" removes those. The done screen's file count covers only the removed project files |
| PRG-57 | Purge Project / Audio Pool Samples | Several slots holding one file share a row | A project has the same sample loaded into two or more unused slots (e.g. S9, S10, S11 all pointing at the same file) | The preview and review tables show one row for that file with all its slot labels joined (e.g. "S9, S10, S11"); the slot count in the status pane and review banner still counts each slot separately |
| PRG-58 | Purge Project / Audio Pool Samples | Action column | Open the preview or review list on a plan mixing removals and slot-only clearings | An Action column shows "Remove" for a file with no slot, "Remove + clear slot" for a removed file whose slot is also cleared, and "Clear slot only" (visually muted) for a slot whose file stays on disk |
| PRG-59 | Purge Project / Audio Pool Samples | Slot count is visible before Execute | Enable "Clear unused sample slot assignments" on a project with unused slots | The Status pane shows "N unused sample slot assignments to clear" alongside the unused-file count, before opening any modal. Unchecking the option removes that line |
| PRG-60 | Purge Project / Audio Pool Samples | Executable with slots but no files | Open a project with zero purgeable audio files but at least one clearable slot (for example every unused slot points into the Audio Pool), with "Clear unused sample slot assignments" on | The Status pane does not claim everything is referenced, and Execute is available. The review banner headlines the slot count and states "no audio files to remove" instead of naming a move destination |
| PRG-61 | Purge Project / Audio Pool Samples | Purge scope selector | Select either purge operation and look at the first option field | A three-way "Purge" selector offers "Unused audio files" (default), "Unused sample slots" and "Both", styled like the Action selector. It replaces the old "Clear unused sample slot assignments" checkbox |
| PRG-62 | Purge Project / Audio Pool Samples | Scope hides options that no longer apply | Switch the Purge selector to "Unused sample slots" | The Action selector, the destination folder field and "Exclude backups/ directory" all disappear - nothing is deleted or moved in that mode. Switching back to "Unused audio files" or "Both" brings them back |
| PRG-63 | Purge Project / Audio Pool Samples | Scope drives what actually runs | Run once with each of the three scopes on a project that has both unused files and clearable slots | "Unused audio files" removes files and leaves every slot untouched; "Unused sample slots" empties the slots and leaves every file on disk; "Both" does the two in one run |
| PRG-64 | Purge Project Samples | Status wording names the scanned scope | Look at the status button for Purge Project Samples | It reads "N unused audio files to purge - of M scanned in project directory" |
| PRG-65 | Purge Audio Pool Samples | Status wording names the scanned scope | Look at the status button with "Include all projects of set" off, then on | Off: "... of M scanned in Audio Pool directory". On: "... of M scanned in Audio Pool and all Projects of Set" |
| PRG-66 | Purge Project / Audio Pool Samples | Slot count sits on the status button | Set the Purge selector to "Both" on a project with both unused files and clearable slots | The status button shows the file line and, on a second line below it, "N unused sample slot assignments to clear". Clicking it opens the list showing both |
| PRG-67 | Purge Project / Audio Pool Samples | Action column wording follows the chosen action | Open the preview list with "Move files to folder" selected, then with "Delete files" | Rows read "Move" / "Move + Clear" in the first case and "Delete" / "Delete + Clear" in the second; slot-only rows always read "Clear slot". In a slots-only run every row reads "Clear slot" |
| PRG-68 | Purge Project / Audio Pool Samples | Action column position | Look at the preview or review table column order | Action sits immediately to the left of Size |
| PRG-69 | All table modals | Show/Hide Columns menu is never clipped | Open the Show/Hide Columns menu (the hamburger) in any table modal, especially one with many columns | The full list of columns is visible with no inner scrollbar and nothing cut off at the bottom, whatever the modal's height |
| PRG-70 | Purge Audio Pool Samples | Slot scopes need the Set's projects in scope | On Purge Audio Pool Samples with "Include all projects of set" off (the default), inspect the Purge selector | "Unused sample slots" and "Both" are visibly disabled and cannot be selected; their tooltip explains that sample slots live in projects, not in the Audio Pool. Checking "Include all projects of set" enables them |
| PRG-71 | Purge Audio Pool Samples | Turning the projects back out of scope resets the selection | Check "Include all projects of set", select "Both" or "Unused sample slots", then uncheck "Include all projects of set" | The selection falls back to "Unused audio files", the slot line disappears from the status button, and executing sends no slot clearing and no project paths |
| PRG-72 | Purge Audio Pool Samples | Pool-only scope wording | With "Include all projects of set" off, read the status button and open both modals | Everything refers to the Audio Pool only: "... of N scanned in Audio Pool directory", and no slot rows or slot counts appear anywhere |
| PRG-73 | Purge Audio Pool Samples | Set-wide file scope wording | Check "Include all projects of set" with Purge = "Unused audio files" | The status button reads "... of N scanned in Audio Pool and all Projects of Set", and the tables list findings from both the pool and the projects |
| PRG-74 | Purge Project / Audio Pool Samples | Slots-only progress screen | Run a purge with Purge = "Unused sample slots" and watch the modal while it works | It reads "Clearing sample slots..." with no "N / M" position, no file name, and no Cancel button - cancellation only ever applies between file units |
| PRG-75 | Purge Project / Audio Pool Samples | Slots-only confirmation screen | Let the same run finish | The summary reads "N unused sample slot assignments cleared", "across N projects" and "no audio file was removed" - with no file count, no destination folder or Trash Bin, and no reclaimed size |
| PRG-76 | Purge Project / Audio Pool Samples | Action column filters | Open the preview or review list on a plan mixing removals and slot-only clearings, then use the Action column's filter icon | The options are worded with the selected action ("Move", "Move + Clear", "Clear slot"); picking one narrows the table to those rows, shows an "Action: ..." badge, and Reset clears it |
| PRG-77 | Purge Project / Audio Pool Samples | Destination path right-click menu | With "Move files to folder" selected, right-click the destination path button | A menu offers "Open in file explorer" (reveals that folder) and "Copy file path" (copies it to the clipboard). Left-clicking elsewhere closes the menu |
| PRG-78 | Purge Audio Pool Samples | Tools panel scrolls | Shrink the window until the Tools tab's options run past the bottom of the page | The panel scrolls so every option and the Execute button stay reachable, as on a project's Tools tab |
| PRG-79 | Purge Project / Audio Pool Samples | Purge selector has no label | Look at the first option field | The three choices appear without a "Purge" caption above them. Disabled choices are dimmed, with a normal cursor (no "forbidden" pointer) |
| PRG-80 | Purge Project / Audio Pool Samples | File count growth is attributed | On a project (or Set) where some files are loaded into slots that never trigger them, note the count with Purge = "Unused audio files", then switch to "Both" | The count grows, and the status line explains the difference as "(N freed by clearing slots)". Switching back to "Unused audio files" removes both the growth and the note |
| PRG-81 | Purge Audio Pool Samples | Exclude backups/ shares the scope row | Check "Include all projects of Set" with Purge set to "Unused audio files" or "Both" | "Exclude backups/ directory" appears as a checkbox on the same line as "Include all projects of Set", to its right - not on a separate line below - making clear it qualifies that scope. Its tooltip says it leaves those backups/ directories out of the unused audio files scan. Unchecking "Include all projects of Set" hides it again |
| PRG-82 | Purge Project Samples | Exclude backups/ shares a row | Select Purge Project Samples with a file scope ("Unused audio files" or "Both") | "Exclude backups/ directory" is a checkbox on the same line as "Review before applying changes", to its right, matching that control exactly. Its tooltip says it leaves this project's backups/ directory out of the unused audio files scan. Switching to "Unused sample slots" removes it and leaves the other checkbox alone on that row |
| PRG-83 | Purge Audio Pool Samples | Scan label wording | Check "Include all projects of Set" and watch the scanning line | It reads "Scanning Audio Pool and Projects... N%" |
| PRG-84 | Purge Project Samples | Never-triggered machines do not protect a slot | Find a slot whose Usage badge reads "referenced in N places but not triggered" (its machine is assigned on a track no pattern trigs), then set Purge to "Unused sample slots" or "Both" | That slot is counted and listed as clearable. The Usage column and the purge tool must never disagree about the same slot |
| PRG-85 | Purge Project Samples | A triggered machine still protects its slot | Find a slot whose Usage badge is the green "played in N places" one | It is never offered for clearing, in any purge scope |
| PRG-86 | Purge Project Samples | Master track does not hold slots hostage | On a project with the Master track enabled (project setting MASTER_TRACK, making T8 the Master track), find a slot whose only reference is a machine on T8 | The Usage badge does not list T8 at all, and the slot is offered for clearing. With the Master track disabled, that same T8 machine counts as normal usage again |
| PRG-87 | Purge Audio Pool Samples | Master track does not hold pool files hostage | Same as PRG-86 but for a pool file whose only Set-wide reference is a T8 machine on a Master-track project | The file counts as unused and is offered for purging |
