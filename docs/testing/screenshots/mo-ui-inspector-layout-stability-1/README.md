# MO-UI-INSPECTOR-LAYOUT-STABILITY-1 screenshots

- **Baseline (before fix):** not captured in automation; compare against START SHA `3c72c5c` on `origin/main` if needed.
- **After fix:** Playwright writes `after-1280-tab-cycle.png` and `after-840-narrow.png` when E2E runs with this directory present.
