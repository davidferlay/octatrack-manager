import { createTranslate } from './messages'

/** Default app locale in tests (matches LocaleProvider wrapper in setup). */
export const tJa = createTranslate('ja')

export const tEn = createTranslate('en')
