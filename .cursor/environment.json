{
  "name": "MasterOCTa",
  "user": "ubuntu",
  "install": "corepack enable && pnpm install --frozen-lockfile && pnpm exec playwright install chromium && cd src-tauri && cargo fetch --locked",
  "terminals": [
    {
      "name": "vite-dev",
      "command": "pnpm run dev"
    }
  ],
  "ports": [1420]
}
